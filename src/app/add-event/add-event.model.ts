export interface AddEventModel {
  latitude: number;
  longitude: number;
  name: string;
  dateTime: Date;
  duration: number;
  category: string;
  minAge: number;
  maxAge: number;
  sex: string;
}
